<div class="flex-none flex justify-center items-center rounded-full w-12 h-12 hover:@BackgroundHoverColor hover:@TextHoverColor cursor-pointer select-none"
    @onclick="OnClick">
    <span class="text-xl mdi mdi-@Icon"></span>
</div>

@code 
{
    [Parameter]
    public string Icon { get; set; } = default!;

    [Parameter]
    public bool DarkMode { get; set; } = false;

    [Parameter]
    public EventCallback<MouseEventArgs> OnClick { get; set; }

    private string BackgroundHoverColor => DarkMode ? "bg-cyan-800" : "bg-gray-200";

    private string TextHoverColor => DarkMode ? "inherit" : "text-cyan-700";
}